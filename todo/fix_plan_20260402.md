# OpenPKPD Fix Plan — 2026-04-02
_Living document. Update status inline as items are completed._

Status legend: `[ ]` not started · `[/]` in progress · `[x]` done · `[-]` deferred

Effort legend: XS < 1 h · S 1–4 h · M 4–8 h · L 1–2 d · XL 3+ d

---

## Overall status

| Sprint | Items | Done | Remaining |
|--------|-------|------|-----------|
| S1 — P1 data/serialisation fixes | 6 | 6 | 0 |
| S1 — C-08 verification | 1 | 1 | 0 |
| S2 — P2 correctness | 10 | 10 | 0 |
| S3 — P3 design & completeness | 16 | 0 | 16 |
| Backlog — P4 debt | 9 | 0 | 9 |

---

## Sprint 1 — P1 critical fixes
_Target: complete before next release cut._
_Ordering: quick data-layer guards first (can be batched in one PR),
then interpolation and serialisation fixes._

### 1a · Data-layer guards (batch — one PR, one test file)
All three live in `event_processor.py`; fix and test together.

- [x] **C-04** · Guard `infusion_end_time` against NaN AMT `(event_processor.py:57–62)` · **XS**
  - Raises `DataError` with subject/time context when `AMT` is NaN and `RATE ≠ 0`

- [x] **C-05** · Reject RATE=−1 when DUR is absent or NaN `(event_processor.py:265)` · **XS**
  - Raises `DataError` when DUR column is absent, NaN, or ≤ 0

- [x] **C-07** · Reject SS=1 with II=0 `(event_processor.py:282)` · **XS**
  - Raises `DataError` with subject/time context; existing ADDL guard unchanged

### 1b · Native ODE interpolation fix
- [x] **C-03** · Boundary observations no longer lost `(code_compiler.py:820–832)` · **S**
  - Added `else: result[sort_idx[ptr]] = y.copy()` to the segment boundary
    guard; mirrors the existing t=0 pattern at line 809

### 1c · Serialisation fix
- [x] **C-06** · Eager user-ODE rebuild in `__setstate__` `(individual.py:468–505)` · **S**
  - `__getstate__` now serialises `_user_ode_param_keys` (picklable tuple of
    strings); `__setstate__` reconstructs a dummy `pk_params` and calls
    `_try_build_user_ode_template` before returning

### 1d · C-08 verification → real FOCEI quad bug found and fixed
- [x] **C-08 / FOCEI quad** · `TestFOCEIFormula` added; real bug fixed `(foce.py:1226)` · **S**
  - Closed-form test revealed the FOCEI quadratic used `r^T Rᵢ^{-1} r` instead
    of `r^T Cᵢ^{-1} r`; added the missing Woodbury correction
    `quad_woodbury = G_T_Rinv_r @ M⁻¹ @ G_T_Rinv_r`; 7 tests pass

---

## Sprint 2 — P2 high-priority correctness
_Target: complete within current sprint after S1 is merged._
_Ordering: one-liner fixes first, then estimation layer, then GUI._

### 2a · One-liner parser / estimation fixes (single PR)

- [x] **H-05** · D-literal regex edge cases `(code_compiler.py:106)` · **XS**
  - Changed `(\d)D([+-]?\d)` → `([0-9.])D([+-]?\d)` to match `1.D0` and `.5D-3`

- [x] **H-07** · Global RNG in NUTS `(nuts.py:265)` · **XS**
  - Replaced `np.random.seed(seed)` with `self._rng = np.random.default_rng(seed)`;
    threaded `rng` through `_build_tree` and all callers for per-chain isolation

- [x] **H-13** · RATE=−2 silently ignored `(event_processor.py:262–267)` · **XS**
  - Added `DataError` guard for `RATE == -2`

### 2b · Estimation layer

- [x] **H-06** · Relative FD step size `(code_compiler.py:745, 856)` · **S**
  - Replaced absolute `h = fd_eps` with `h = fd_eps * max(abs(x), 1.0)` in
    both the PK-parameter and ETA sensitivity probes

- [x] **H-08** · Laplacian OFV missing `log|Ω|` `(laplacian.py:111–152)` · **M**
  - Added `log_det_omega = np.linalg.slogdet(omega)[1]` computed once per outer
    iteration; added to the per-subject OFV accumulation

- [x] **H-01** · ADVAN5 near-degenerate eigenvalues `(advan5.py:131–153)` · **M**
  - `_eigendecomp_n` returns `P_inv=None` when `cond(P) > 1e10`; `_propagate_ncmt`
    and `_infusion_response_n` fall back to `scipy.linalg.expm` when `P_inv is None`

### 2c · Data layer (complement to S1)

- [x] **H-04** · F1 not applied for ADVAN5/7/11 oral inputs `(advan5.py, advan7.py)` · **S**
  - Added `f1 = float(pk_params.get("F1", 1.0))` in each solver's `solve()` method;
    bolus amounts and infusion rates scaled by `f1` before superposition

### 2d · GUI correctness

- [x] **H-12** · PK ↔ OMEGA dimension check `(model_translation_service.py)` · **S**
  - Regex-counts max `ETA(n)` index in `pk_code`; surfaces a `ValidationIssue`
    when `max_eta > eta_count`; `import re` added to file

- [x] **H-10** · Stale dataset shown on CSV load failure `(data_workflow.py:708–711)` · **S**
  - On load failure path, replaced `_render(project.active_dataset, …)` with
    `_render(None, …)` to clear columns and preview panels

- [x] **H-11** · Mode switch discards edits silently `(model_workflow.py)` · **S**
  - Added `last_confirmed_mode` container; `_handle_mode_changed` shows
    `QMessageBox.question` when `_has_unsaved_changes()` is True and reverts
    the radio button if the user cancels

- [x] **M-13** · ParseError loses line context in GUI `(model_translation_service.py)` · **S**
  - Formats `str(exc).split("\n")[0]` + `exc.context.strip()` as a single-line
    message; multi-line context no longer breaks the `QListWidget` display

_Bonus fix:_ `dataset.py` — NONMEM `"."` missing-value placeholder was incorrectly
counted as a non-numeric value by the Sprint-1 column-dtype guard; now replaced with
`np.nan` before the check so example datasets with `DV=.` on dosing rows pass.


---

## Sprint 3 — P3 design & completeness
_Target: schedule across next two sprints; group by domain._

### 3a · Parser / compiler

- [ ] **M-05** · IF-THEN-ELSE ENDIF pairing validation `(code_compiler.py:198–222)` · **S**
  - Count THEN/ENDIF pairs during parsing; raise `CompilerError` on mismatch
  - Test: feed an unmatched ENDIF; assert `CompilerError` is raised

### 3b · Model layer

- [ ] **M-01** · `IndividualModel` God-Object split `(individual.py, ~2 455 lines)` · **XL**
  - Split into functional mixins: `PKSolutionMixin`, `ObservationModelMixin`,
    `LikelihoodMixin`, `DerivativesMixin`; `IndividualModel` composes them
  - **Prerequisite:** M-02 and M-03 must be merged first to reduce conflict surface
  - Test: all existing Section 18 and estimation unit tests must pass unchanged

### 3c · Native ODE / model dispatch

- [ ] **M-02** · Extract `_select_template` to remove 4× redundant loops `(individual.py:685,826,970,1156)` · **M**
  - Introduce `_select_template(pk_params) → _NativeOdeTemplate | None` cached
    per `pk_params` signature; replace the four inline loops
  - Test: existing Section 18 tests must pass without change

- [ ] **M-03** · Covariate time-constancy check is over-broad `(individual.py:618)` · **S**
  - Replace bare `except Exception: return None` with explicit numeric check;
    skip columns not in the PK code token set
  - Test: model with an unused categorical covariate; assert native path is NOT disabled

### 3c · Residual / error models

- [ ] **M-04** · Power residual discontinuous at f = 0 `(residual_models.py:217–219)` · **S**
  - Replace `np.finfo(float).tiny` with `sigma_sq * abs(f) ** (2 * theta)`;
    remove the special-case branch
  - Test: assert `var(f=−0.01)` is continuous with `var(f=0.01)` to 6 s.f.

### 3d · PK subroutines

- [ ] **M-06** · ADVAN12 relative eigenvalue tolerance `(advan12.py:100–120)` · **S**
  - Replace literal `1e-8` with `rtol * max(abs(lam_j), 1e-30)` (`rtol=1e-6`)
    at all three occurrence sites
  - Test: model with `KA = 200 h⁻¹`; assert output matches numerical ODE

### 3e · Estimation

- [ ] **M-14** · IMP adaptive isample on low ESS `(imp.py:482)` · **M**
  - If `ESS / isample < 0.1` double isample for that subject and resample;
    cap at `4 × isample_max`
  - Test: synthetic subject with peaked likelihood; assert ESS > 0.1 × isample

### 3f · Test coverage

- [ ] **M-07** · Mixture model EM convergence test `(tests/unit/mixture/)` · **S**
  - Fit bimodal synthetic data (two Gaussians, n=200) with `MixtureEM`;
    assert recovered means within 10 % and convergence in < 100 iterations

- [ ] **M-08** · SAEM external validation vs NONMEM `(tests/external_validation/)` · **L**
  - Run NONMEM SAEM on theophylline; store reference output; assert OpenPKPD
    THETAs within 5 % and OFV within 2 units

### 3g · GUI

- [ ] **M-15** · VPC / NPDE bootstrap confidence bands `(vpc.py, npde.py)` · **L**
  - Add `n_bootstrap` parameter; return lower/upper CI arrays per percentile curve
  - Test: assert CI width > 0 and CI contains the point estimate

### 3h · Documentation

- [ ] **M-09** · Stratified bootstrap resampling `(bootstrap.py:454–488)` · **M**
  - Add `stratify_by: list[str] | None`; resample within strata
  - Test: dose-stratified resample preserves dose-group proportions within ±1 subject

- [ ] **M-10** · Stub pages for 21 undocumented example scripts `(docs/examples/)` · **S**
  - One `.md` stub per missing script; title, one-sentence description, link to `.py`

- [ ] **M-11** · Estimation method decision guide `(docs/user_guide/estimation_methods.md)` · **S**
  - "Choosing a method" section with Mermaid flowchart; cover data size,
    continuous vs TTE, frequentist vs Bayesian

- [ ] **M-12** · Document `impute_covariates`, `design`, `simulate` `(docs/user_guide/model_builder.md)` · **S**
  - Narrative sections with parameter descriptions and worked examples

---

## Backlog — P4 low-priority debt
_Address opportunistically; no sprint commitment._

- [ ] **L-03** · Reuse eigenvalues from `repair_pd` in inversion `(individual.py:2482)` · **XS**
  - `omega_inv = (evecs / np.maximum(evals, eps)) @ evecs.T`

- [ ] **L-04** · Parallel backend smoke tests `(tests/unit/parallel/)` · **M**
  - Ray/Dask init, timeout, and retry paths

- [ ] **L-05** · TTE / TMDD external validation `(tests/external_validation/)` · **L**
  - Synthetic reference datasets compared against nlmixr2 or NONMEM

- [ ] **L-06** · Extract `_render_validation_issues` helper across four workflows · **S**
  - Single function in `openpkpd_gui/widgets/validation_list.py`

- [ ] **L-08** · EVID=5 explicit error `(event_processor.py)` · **XS**
  - `raise NotImplementedError("EVID=5 is not yet supported")`

- [ ] **L-09** · Document ADVAN9/14/15 absence in ADVAN reference page · **XS**
  - Note unsupported ADVANs; direct users to ADVAN6/13 custom ODE

- [ ] **L-10** · BAYES / NUTS external validation `(tests/external_validation/)` · **M**
  - Normal–normal conjugate model; compare posterior mean/SD against PyMC

- [ ] **L-11** · Visual regression tests for plots `(tests/unit/plots/)` · **M**
  - Adopt `pytest-image-diff`; add baselines for VPC, GOF, forest plot

- [ ] **L-12** · Decompose `build_model_workflow` (1 253 lines) `(model_workflow.py:693–)` · **L**
  - Extract `_build_parameter_tables`, `_build_code_editors`,
    `_build_estimation_section`, and `ModelWorkflowEventHandler`

---

## Dependency notes

```
C-04, C-05, C-07  →  batch in one PR (all in event_processor.py)
C-03              →  independent; native Numba path only
C-06              →  independent; parallel workers only
C-08              →  write test first; fix only if test fails
H-08              →  independent; Laplacian path only
H-01              →  verify ADVAN7 is importable from advan5.py before starting
M-02              →  should land before M-03 (both touch individual.py dispatch)
M-01              →  large refactor; defer until M-02 and M-03 are merged
M-09 (bootstrap)  →  independent of M-15 (VPC CI)
```
